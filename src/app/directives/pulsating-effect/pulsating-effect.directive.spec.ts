import { PulsatingEffectDirective } from './pulsating-effect.directive';

describe('PulsatingEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new PulsatingEffectDirective();
    expect(directive).toBeTruthy();
  });
});
